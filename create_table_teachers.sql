CREATE TABLE teachers (
    teacher_id integer PRIMARY KEY,
    surname varchar(30),
    name varchar(30),
    employment_date date,
    date_of_birth date,
    sex char(1),
    salary float,
    workload integer,
    phone_no varchar(10)
);

